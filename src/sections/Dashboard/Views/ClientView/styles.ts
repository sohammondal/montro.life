import styled, { css } from 'styled-components'

import { flexColCenter } from 'theme'

export const containerStyles = {
  outerStyles: css``,
  innerStyles: css`
    width: auto;
    height: 85vh;
    ${flexColCenter}
    justify-content: space-evenly;
  `,
}

export const qrCodeBtnCss = css`
  margin-top: 15px;
`

export const ChartWrapper = styled.div``
