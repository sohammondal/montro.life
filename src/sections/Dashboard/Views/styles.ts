import { css } from 'styled-components'

import { flexColCenter } from 'theme/common.styles'

export const containerStyles = {
  outerStyles: css``,
  innerStyles: css`
    width: auto;
    height: 70vh;
    ${flexColCenter}
  `,
}
